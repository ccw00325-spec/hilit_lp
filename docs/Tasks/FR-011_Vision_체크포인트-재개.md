---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[Command] FR-011: 처리 단계 체크포인트 저장 및 재개"
labels: 'backend, command, vision, priority:high, step-2'
assignees: ''
---

> ### 🔄 SRS v3.0 반영 — 이 태스크에서 바뀐 것 *(2026-09-01)*
> **체크포인트 단위 변경** — 원본 단위 단일 진행에서 **① 컷 → ② 클립별 추적 → ③ 복구 → ④ 선택** 4단계로 늘었다. 🔴 **R3**: 추적은 **클립 단위로 즉시 저장**한다 — 브라우저 탭이 닫혀도 끝난 클립은 남는다.
>
> **기준 문서**: `SRS/[SRS]hilit-SRSv2.0-nextjs.md` **v3.0** · `PRD/HILiT_PRD_v0_2.md`

> ### 🔴 SRS v3.2 반영 — 체크포인트가 "복귀 지점"까지 들고 있어야 한다 *(2026-09-02)*
> PRD v0.3 **F29**는 사용자가 대기 중 **피드로 나갔다가 돌아오는 것**을 정상 경로로 만든다. 그러면 이 태스크가 답해야 할 질문이 하나 늘어난다 — *"어디까지 끝났는가"* 에 더해 🔴 ***"돌아오면 어느 화면인가"***.
>
> | 신설 필드 | 뜻 |
> | --- | --- |
> | **`resume_route`** | 🔴 **복귀 지점** — 단계 전이와 **같은 트랜잭션**에서 쓴다. 따로 쓰면 그 사이에 사용자가 복귀했을 때 옛 경로로 간다 |
> | **`progress_num` · `progress_den`** | 미니바의 퍼센트 근거 — 추적은 클립 수, 컷은 2단계 중 몇 번째 |
> | **`notified_at`** | 완료 알림이 사용자에게 **도달**한 시각 |
>
> 🔴 **이 태스크는 여전히 상태를 "만드는" 쪽이다.** 전달은 FR-036, 표시는 FR-046이 맡는다 — 경계가 흐려지면 미니바가 상태를 고치기 시작한다.
>
> **기준 문서**: `SRS/[SRS]hilit-SRSv2.0-nextjs.md` **v3.2 §4.1** · `PRD/HILiT_PRD_v0_2.md` **v0.3** F29 · **ST-5**

## 🎯 Summary
- 기능명: **[FR-011] 앱을 닫았다 열어도 처리가 처음부터 다시 시작되지 않는다**
- 목적: **40분 원본의 처리 도중 이탈이 비용과 시간의 전손으로 이어지지 않게 한다.**

> 🔴 **이 태스크의 근거는 사용자 여정에 있다.**
>
> v1.8 §3의 이탈 지점 분석은 **④ 편집 단계에서 30분을 넘기면 사용자가 포기**한다고 기록했다. 탐지는 p95 8분이 목표지만(REQ-NF-003) **사용자가 그 8분을 앱 안에서 기다리지 않는다.** 앱을 닫는 것이 정상 경로다.
>
> **체크포인트가 없으면 정상 경로가 실패 경로가 된다.**

## 🔗 References (Spec & Context)
> 💡 AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- **시나리오**: `SRS/[SRS]hilit-SRSv1.8.md` **SC-1.F4** 🆕 — 🔴 **처리 중 앱 종료 → 마지막 완료 단계에서 재개**
- **요구사항**: `SRS/[SRS]hilit-SRSv1.8.md` **REQ-NF-008** — 🔴 **원본·중간 산출물을 보존한 채 재시도 3회 이상**
- **상태 기계**: `SRS/[SRS]hilit-SRSv1.8.md` §7.2 — 편집 파이프라인 6단계
- **엔티티**: `SRS/[SRS]hilit-SRSv1.8.md` §6.2 `ProcessingJob.stage`
- **이탈 분석**: `SRS/[SRS]hilit-SRSv1.8.md` §3 — ④ 편집 2차 이탈
- **완주율**: `SRS/[SRS]hilit-SRSv1.8.md` **REQ-NF-014** — 파이프라인 완주율 ≥ 95%
- 실시간 전달: `SRS/[SRS]hilit-SRSv2.0-nextjs.md` §5.1 — Realtime 구독(FR-036)

## ✅ Task Breakdown (실행 계획)
- [ ] `processing_jobs.stage` **전이 시점마다 커밋** — 단계 완료를 즉시 영속화
- [ ] 🔴 **단계별 중간 산출물 보존** — 재개 시 이전 단계를 다시 하지 않도록
- [ ] **재진입 시 상태 조회** — 어느 단계까지 끝났는지 반환
- [ ] 🔴 **재시도 3회** 구현 — REQ-NF-008
- [ ] **재시도 대상 오류 분류** — 일시 오류만 재시도, 영구 오류는 즉시 실패
- [ ] `failure_class` 기록 — `CAPTURE` / `INFRA` 구분
- [ ] 🔺 **재시도 소진 후 처리** — 실패 상태와 사용자 안내 경로
- [ ] 계측 — `job_resumed` · `job_retried` · **완주율 집계 입력**
- [ ] 🔴 **`resume_route` 를 단계 전이와 같은 트랜잭션에서 기록** *(v3.2 · F29)*
- [ ] 🔴 **`progress_num` / `progress_den` 갱신** *(v3.2)* — 추적은 `submitTrack` 마다 분자 증가
- [ ] `notified_at` 컬럼 제공 — 🔺 **도달 시각을 누가 쓰는지는 FR-046이 정한다**(DS D-7)

## 🧪 Acceptance Criteria (BDD/GWT)

**Scenario 1: 앱을 닫아도 처리가 이어진다** *(SC-1.F4)*
- **Given**: 탐지 진행 중 앱을 종료함
- **When**: 나중에 앱을 다시 엶
- **Then**: 🔴 **마지막 완료 단계에서 재개**된다. 처음부터 다시 시작하지 않는다

**Scenario 2: 재개가 아니라 계속이다**
- **Given**: 탐지는 외부 서비스에서 진행 중(FR-005)
- **When**: 앱을 닫음
- **Then**: 🔴 **추론 자체는 중단되지 않는다.** 앱은 결과 수신처가 아니라 **조회자**이며, 결과는 webhook(FR-005)으로 들어온다. **"재개"의 실체는 화면 복원이다**

**Scenario 3: 실패해도 중간 산출물이 남는다** *(REQ-NF-008)*
- **Given**: 리프레이밍 단계에서 실패
- **When**: 재시도를 수행함
- **Then**: 🔴 **탐지 결과를 다시 만들지 않는다.** 원본과 이전 단계 산출물이 보존되어 있다

**Scenario 4: 재시도가 3회 수행된다** *(REQ-NF-008)*
- **Given**: 일시적 오류
- **When**: 처리가 실패함
- **Then**: **최대 3회 재시도**되고 각 시도가 `job_retried` 로 계측된다

**Scenario 5: 촬영 실패를 재시도하지 않는다**
- **Given**: 등장 구간 0건(`failure_class = CAPTURE`)
- **When**: 재시도 판정을 함
- **Then**: 🔴 **재시도하지 않는다.** 같은 원본에 같은 anchor로 다시 돌려도 결과가 같다. **비용만 3배가 된다** — 사용자에게 재지정(FR-005)을 안내해야 한다

**Scenario 6: 완주율이 산출된다** *(REQ-NF-014)*
- **Given**: 단계 전이 이벤트
- **When**: 집계함
- **Then**: **업로드 시작 대비 기록 생성 완료 비율**이 산출된다. 🔺 **95%는 목표값이며 이 태스크는 측정 가능성까지 책임진다**

**Scenario 7 (실패): 재시도 소진이 조용히 끝나지 않는다**
- **Given**: 3회 재시도 후에도 실패
- **When**: 상태를 확인함
- **Then**: 🔴 **`FAILED` 로 확정되고 사용자에게 안내가 도달**한다. 무한 대기 화면이 남지 않는다

## ⚙️ Technical & Non-Functional Constraints
- 🔴 **A-T3(장시간 워커 불가)가 설계를 규정한다** — 서버에 상주하는 재개 데몬을 둘 수 없다. **재개는 사용자 재진입 또는 webhook 도착이 트리거한다**
- 🔴 **`CAPTURE` 는 재시도 대상이 아니다** — 인프라 실패와 촬영 실패를 같은 재시도 정책으로 다루면 원가가 새고 실패 통계가 오염된다
- 🔺 **재시도 간격이 `[TBD]`** — 즉시 / 지수 백오프 중 선택. 외부 API의 속도 제한과 함께 정한다
- 단계 전이는 **원자적으로** — 부분 전이 상태가 남으면 재개 지점을 알 수 없다
- 진행 상태의 사용자 전달은 **FR-036(Realtime)** 소관이다. 이 태스크는 **상태를 정확히 남기는 것**까지

## 🏁 Definition of Done (DoD)
- [ ] 모든 Acceptance Criteria를 충족하는가?
- [ ] 🔴 **앱 종료 후 재진입 시 마지막 완료 단계에서 이어지는가?**
- [ ] 🔴 **실패해도 중간 산출물이 보존되는가?**
- [ ] 🔴 **`CAPTURE` 가 재시도 대상에서 제외되는가?**
- [ ] 재시도 3회가 동작하고 계측되는가?
- [ ] 재시도 소진 시 `FAILED` 로 확정되고 안내되는가?
- [ ] 완주율 집계의 입력 이벤트가 발행되는가?
- [ ] 상주 워커에 의존하지 않는가? *(A-T3)*
- [ ] TypeScript strict · ESLint 경고 0건인가?

## 🚧 Dependencies & Blockers
- **Depends on**: **CT-001**(`processing_jobs`) · **FR-005**(Job 등록)
- **Blocks**: **FR-036**(Realtime 구독) · **NF-003**(완주율 집계) · **TS-007**
- **연관**: **FR-016**(렌더 · 이탈 대응)이 클라이언트 측의 같은 문제를 다룬다 — 🔴 **렌더는 브라우저에서 돌므로 탭 종료 시 실제로 소실된다**(v2.2 §6.5.3). **이 태스크와 대칭이 아니다**
